import * as auth from './authManager'
import * as accounts from './accountsManager'
import * as transactions from './transactionsManager'

export const authManager = auth
export const accountsManager = accounts
export const transactionsManager = transactions
